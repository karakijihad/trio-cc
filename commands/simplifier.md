---
description: Audit through the simplifier lens only
argument-hint: [--max N]
---

Use the `trio-audit` skill to run the loop with `--lenses simplifier`. Pass
through any other arguments the operator supplied: $ARGUMENTS
