---
description: Audit through the consistency lens only
argument-hint: "[--max N]"
---

Use the `trio-audit` skill to run the loop with `--lenses consistency`. Pass
through any other arguments the operator supplied: $ARGUMENTS
