---
description: Audit through the tester lens only
argument-hint: [--max N]
---

Use the `trio-audit` skill to run the loop with `--lenses tester`. Pass
through any other arguments the operator supplied: $ARGUMENTS
