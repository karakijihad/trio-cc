---
description: Show the Trio control panel
---

Run `node "${CLAUDE_PLUGIN_ROOT}/bin/trio.mjs" status` and show the operator its
output verbatim in a code block. Do not summarise or reformat it; the panel is
already formatted.

If the panel reports a drift warning or a preflight problem, add one sentence
naming the single command that fixes it.
