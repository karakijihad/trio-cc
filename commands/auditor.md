---
description: Audit through the auditor lens only
argument-hint: [--max N]
---

Use the `trio-audit` skill to run the loop with `--lenses auditor`. Pass
through any other arguments the operator supplied: $ARGUMENTS
